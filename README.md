# DTracker (robust sync) — ?customer parameter

- Debounced + periodic state sync (every change + 1s diff)
- Presence logs errors in console to help debug rules/auth

Deploy steps are identical to prior builds.
